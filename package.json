{
  "name": "notfall-webpage",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "pnpm --filter @workspace/stitch-bg run build",
    "dev": "pnpm --filter @workspace/stitch-bg run dev",
    "serve": "pnpm --filter @workspace/stitch-bg run serve",
    "typecheck": "pnpm --filter @workspace/stitch-bg run typecheck"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
